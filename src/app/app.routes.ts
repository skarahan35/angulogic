import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

export const routes: Routes = [
  { path: 'sidebar', component: AppComponent },
  { path: 'test', component: AppComponent },
];
